# rvz-performance

Optional NumPy and CFFI padding generators for the `rvz` package.

Most users should install this through the core package:

```text
pip install rvz[performance]
```

The package can also be used directly through the `rvz_performance` import.
